;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/zkim/.emacs.d/elpa/eproject-0.4")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 22)
        (left . 0)
        (width . 132)
        (height . 47)))

